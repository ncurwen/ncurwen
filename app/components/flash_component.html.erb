<div id="<%= "notification-#{SecureRandom.uuid}" %>"
      role="alert"
      class="alert <%= type_class %> transition transform duration-300 hidden flex flex-col gap-0"
      data-controller="flash-component"
      data-flash-component-auto-dismiss-value="<%= auto_dismiss %>"
      data-transition-enter-from="opacity-0 translate-x-6"
      data-transition-enter-to="opacity-100 translate-x-0"
      data-transition-leave-from="opacity-100 translate-x-0"
      data-transition-leave-to="opacity-0 translate-x-6"
      data-turbo-permanent>
  <div class="flex w-full justify-between">
    <div class="flex gap-2 justify-left items-start mt-1 mr-2">
      <%= lucide_icon(type_icon, class: "w-5 h-5 shrink-0 #{icon_hide}".strip) %>
      <span>
        <p><%= message %></p>
        <%= action_button %>
      </span>
    </div>
    <%# No tooltip: the toast dismisses itself, and aria-label already names the
        button for anyone who needs it. %>
    <button class="btn btn-xs btn-outline btn-square mt-0.5"
            type="button"
            data-action="flash-component#hide"
            data-dismiss="alert"
            aria-label="Close">
      <%= lucide_icon("x", class: "w-3 h-3") %>
    </button>
  </div>
  <% if auto_dismiss %>
    <%# max is coupled to the controller's delay value: 300 ticks x 20ms = the
        6000ms default. Change one and the bar desyncs from the dismissal. %>
    <progress class="progress h-0.5 mt-1.5" value="0" max="300" data-flash-component-target="progress"></progress>
  <% end %>
</div>
